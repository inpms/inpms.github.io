## 2024

#### 设置抽奖活动 （已修改）

设置抽奖活动：https://fengcheng.fm-community.com.cn/o2o-question-web/award/activityList.do

（新增|修改接口）提供一下具体的接口地址

  https://fengcheng.fm-community.com.cn/o2o-question-web/award/activityList.do  

 addActivity: net + "/o2o-question-web/award/addActivity.do", //新增活动
​         editActivity: net + "/o2o-question-web/award/editActivity.do", //编辑活动

/o2o-question-web/award/editActivityItem.do  



#### H5 

#### 请求是否有抽奖资格(已修改)

"/o2o-question-webAndroid/awardAndroid/luckPreconditions.do", //请求是否有抽奖资格

根据抽奖Version 来判断 前端添加version (1,2)字段，参考：

Long houseidRequest = jsonObjectData.getLong("houseid");

把物业费放开，直接给你true,表示可以抽奖, V2(version ==2)



#### 根据物业券的使用时间，查询当前是否有历史的账单（使用条件）(已修改)

新增逻辑；使用优惠券的时候，需要检测用户是否有账单，使用的前置条件

状态：已使用|已获得|未使用|等

"/o2o-assetsv2-webAndroid/android/my_paylist.do", //物业缴费，获取应缴费用



#### 获取所有卡券列表（物业|车费）(已修改)

NSString *const kMyAllCardListPath = @"o2o-common-webAndroid/android/selectMyCouponList.do";

添加使用条件，确认一下是否有限制条件



#### 开始抽奖 (已修改)

lottery_start: prefix + "/o2o-question-webAndroid/awardAndroid/luck.do", //开始抽奖  有没有校验房产

这里控制了房产，这里需要带上抽奖的版本（version 1, 2 ）,为2，新逻辑，不为2，保持之前的逻辑

<if test="houseId != null and houseId == 0 ">

​            and house_id = #{houseId}

​        </if>

